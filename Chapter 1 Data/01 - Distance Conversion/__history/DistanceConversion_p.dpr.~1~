program pDistanceConversion;

uses
  Forms,
  uDistanceConversion in 'uDistanceConversion.pas' {frmDistanceConversion};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TfrmDistanceConversion, frmDistanceConversion);
  Application.Run;
end.
